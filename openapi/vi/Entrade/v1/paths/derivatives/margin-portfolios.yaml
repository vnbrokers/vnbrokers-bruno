get:
  operationId: listDerivativeMarginPortfolios
  tags:
    - Phái sinh
  summary: Lấy danh sách gói vay
  description: Lấy danh sách gói vay theo investorId.
  parameters:
    - name: investorId
      in: path
      required: true
      schema:
        type: number
  responses:
    "200":
      description: Thành công
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/derivatives/MarginPortfolioPage.yaml
    "400":
      $ref: ../../components/responses/BadRequest.yaml
    "500":
      $ref: ../../components/responses/InternalError.yaml
