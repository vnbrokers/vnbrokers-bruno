type: object
additionalProperties: false
required: [method, channel, topics]
properties:
  method:
    type: string
    const: SUBSCRIBE
  channel:
    type: string
    const: TRADING
  topics:
    type: array
    minItems: 1
    uniqueItems: true
    description: Danh sách topic sự kiện lệnh.
    items:
      type: string
      minLength: 1
      pattern: '^order\.(\*|[A-Za-z0-9_-]+(?:-[A-Za-z0-9_-]+)*)$'
