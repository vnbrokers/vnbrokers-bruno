name: subscribeMarketIndexRequest
title: Đăng ký luồng chỉ số thị trường
summary: Đăng ký topic `market_index.{index_name}.json`.
payload:
  $ref: ../../schemas/market_index/SubscribeRequest.yaml
examples:
  - name: subscribeMarketIndex
    payload:
      action: subscribe
      channels:
        - name: market_index.VN30.json
