name: subscribeBrokerPositionRequest
title: Đăng ký luồng vị thế môi giới
summary: Đăng ký topic `position.broker.{market_type}.{investor_id}.json`.
payload:
  $ref: ../../schemas/position_broker/SubscribeRequest.yaml
examples:
  - name: subscribeBrokerPosition
    payload:
      action: subscribe
      channels:
        - name: position.broker.STOCK.1000009250.json
          symbols: []
