type: object
additionalProperties: false
required: [name, symbols]
properties:
  name:
    type: string
    description: Topic độ sâu thị trường theo nhóm thị trường.
    pattern: '^top_price\.[A-Z0-9]+\.json$'
  symbols:
    type: array
    minItems: 1
    uniqueItems: true
    items: { type: string, minLength: 1 }
