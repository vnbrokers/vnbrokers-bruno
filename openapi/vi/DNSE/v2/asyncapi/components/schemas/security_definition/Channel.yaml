type: object
additionalProperties: false
required: [name, symbols]
properties:
  name:
    type: string
    description: Topic thông tin chứng khoán theo nhóm thị trường.
    pattern: '^security_definition\.[A-Z0-9]+\.json$'
  symbols:
    type: array
    minItems: 1
    uniqueItems: true
    items: { type: string, minLength: 1 }
