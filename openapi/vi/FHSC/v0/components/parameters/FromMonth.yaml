name: from-month
in: query
required: false
description: |
  Tháng bắt đầu, format `yyyy-MM` (ví dụ `2024-01`). Bắt buộc khi `period`
  bị bỏ qua trên các endpoint benchmark `nav` / `operation`.
schema:
  type: string
  example: 2024-01
