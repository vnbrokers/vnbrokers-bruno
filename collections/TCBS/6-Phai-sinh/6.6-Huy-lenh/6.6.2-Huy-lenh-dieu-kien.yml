info:
  name: 6.6.2. Huỷ lệnh điều kiện
  type: http
  seq: 2
  tags:
    - cancel_order_derivative
http:
  method: POST
  url: "{{baseUrl}}/khronos/v1/order/condition/cancel"
  body:
    type: json
    data: |-
      {
        "accountId": "105C031402",
        "subAccountId": "105C031402A",
        "orderNo": "1"
      }
  auth: inherit
runtime:
  scripts:
    - type: tests
      code: |-
        test("response matches the schema", function () {
          const schema = {
            "$schema": "http://json-schema.org/draft-07/schema#",
            "type": "object",
            "properties": {
              "cmd": {
                "type": "string"
              },
              "rc": {
                "type": "string"
              },
              "rs": {
                "type": "string"
              },
              "oID": {
                "type": "string"
              },
              "data": {
                "type": "array",
                "items": {}
              }
            }
          }

          res.status === 200 && expect(res.body).to.have.jsonSchema(schema);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: |-
      OK Loại bút toán 1: success, < 0: error Chi tiết lỗi
    request:
      url: "{{baseUrl}}/khronos/v1/order/condition/cancel"
      method: POST
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "accountId": "105C031402",
            "subAccountId": "105C031402A",
            "orderNo": "1"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "cmd": "Web.cancelActivatedOrder",
            "rc": "1",
            "rs": "",
            "oID": "",
            "data": []
          }
