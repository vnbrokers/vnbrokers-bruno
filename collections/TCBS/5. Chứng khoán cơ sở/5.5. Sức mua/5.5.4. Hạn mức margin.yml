info:
  name: 5.5.4. Hạn mức margin
  type: http
  seq: 4
  tags:
    - purchasing_power_stock_normal

http:
  method: GET
  url: "{{baseUrl}}/aion/v1/customers/:custodyId/accounts"
  params:
    - name: custodyId
      value: "{{custodyCode}}"
      type: path
      description: Số tài khoản
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
