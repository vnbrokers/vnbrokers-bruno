info:
  name: trade-ppmmrAccount
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v3/trading/ppmmrAccount?clientId={{clientId}}&accountNo={{accountNo}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("response returns success", function () {
          expect(res.status).to.be.oneOf([200]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |- 
  ### GET /api/v3/trading/ppmmrAccount

  Lấy thông tin PP/MMR của tài khoản.

  Authentication: Bearer token.