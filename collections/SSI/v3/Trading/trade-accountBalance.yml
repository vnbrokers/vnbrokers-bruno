info:
  name: trade-accountBalance
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v3/trading/accountBalance?clientId={{clientId}}&accountNo={{accountNo}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("response returns success", function () {
          expect(res.status).to.be.oneOf([200]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |- 
  ### GET /api/v3/trading/accountBalance

  Lấy số dư tài khoản giao dịch.

  Authentication: Bearer token.