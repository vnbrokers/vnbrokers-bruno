info:
  name: FastConnect Data
  type: folder
  seq: 2

request:
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
      disabled: true
  auth:
    type: bearer
    token: "{{accessToken}}"
