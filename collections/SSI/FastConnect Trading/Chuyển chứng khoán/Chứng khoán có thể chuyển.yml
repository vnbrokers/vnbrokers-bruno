info:
  name: Chứng khoán có thể chuyển
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v2/stock/transferable"
  params:
    - name: account
      value: "{{account}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
