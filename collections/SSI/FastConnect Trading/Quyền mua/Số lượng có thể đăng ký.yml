info:
  name: Số lượng có thể đăng ký
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v2/ors/exercisableQuantity"
  params:
    - name: account
      value: "{{account}}"
      type: query
    - name: instrumentID
      value: "{{instrumentID}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
