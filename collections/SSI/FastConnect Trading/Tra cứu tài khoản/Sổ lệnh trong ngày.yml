info:
  name: Sổ lệnh trong ngày
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v2/Trading/OrderBook"
  params:
    - name: account
      value: "{{account}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
