info:
  name: Số dư tài khoản cơ sở
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v2/Trading/cashAcctBal"
  params:
    - name: account
      value: "{{account}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
