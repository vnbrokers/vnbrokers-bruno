info:
  name: Danh mục chứng khoán cơ sở
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v2/Trading/stockPosition"
  params:
    - name: account
      value: "{{account}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
