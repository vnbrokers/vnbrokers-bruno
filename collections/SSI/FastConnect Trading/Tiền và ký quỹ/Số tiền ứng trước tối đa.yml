info:
  name: Số tiền ứng trước tối đa
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v2/cash/cashInAdvanceAmount"
  params:
    - name: account
      value: "{{account}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
