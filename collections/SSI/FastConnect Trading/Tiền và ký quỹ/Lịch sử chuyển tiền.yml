info:
  name: Lịch sử chuyển tiền
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v2/cash/transferHistories"
  params:
    - name: account
      value: "{{account}}"
      type: query
    - name: startDate
      value: "{{startDate}}"
      type: query
    - name: endDate
      value: "{{endDate}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
