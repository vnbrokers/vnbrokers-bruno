info:
  name: Ước tính phí ứng trước
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v2/cash/estCashInAdvanceFee"
  params:
    - name: account
      value: "{{account}}"
      type: query
    - name: ciaAmount
      value: "{{ciaAmount}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
