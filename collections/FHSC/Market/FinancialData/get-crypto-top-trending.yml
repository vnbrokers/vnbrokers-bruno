info:
  name: Top trending Crypto (GET)
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/market/financial-data/cryptos/top-trending"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true

docs: |-
  ### GET /market/financial-data/cryptos/top-trending

  Danh sách các loại tiền điện tử đang trending.

  **Response key:** `data`
