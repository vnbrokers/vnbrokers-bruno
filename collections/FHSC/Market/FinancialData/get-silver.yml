info:
  name: Giá bạc (GET)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/market/financial-data/silver"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true

docs: |-
  ### GET /market/financial-data/silver

  Giá bạc thời gian thực.

  **Response key:** `data`
