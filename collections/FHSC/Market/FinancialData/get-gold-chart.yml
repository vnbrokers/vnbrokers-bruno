info:
  name: Biểu đồ vàng (GET)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/market/financial-data/gold-chart"
  params:
    - name: days
      value: "30"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true

docs: |-
  ### GET /market/financial-data/gold-chart

  Dữ liệu lịch sử giá vàng trong N ngày.

  **Params:**
  | Param | Type | Default | Description |
  |-------|------|---------|-------------|
  | days | number | 30 | Số ngày |

  **Response key:** `data`
