info:
  name: Giá kim loại theo nhà cung cấp (GET)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/market/financial-data/metal-providers"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true

docs: |-
  ### GET /market/financial-data/metal-providers

  Giá bạc và kim loại khác theo nhà cung cấp.

  **Response key:** `data`
