info:
  name: Giá vàng (GET)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/market/financial-data/gold"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true

docs: |-
  ### GET /market/financial-data/gold

  Giá vàng SJC và vàng thế giới thời gian thực.

  **Response key:** `data`
