info:
  name: Lãi suất ngân hàng (GET)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/market/financial-data/bank-interest-rates"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true

docs: |-
  ### GET /market/financial-data/bank-interest-rates

  Lãi suất tiền gửi ngân hàng hiện tại.

  **Response key:** `data`
