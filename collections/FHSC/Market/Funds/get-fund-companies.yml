info:
  name: Công ty quản lý quỹ
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/fund-trading/public/fund-companies"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true

docs: |-
  ### GET /fund-trading/public/fund-companies

  Danh sách công ty quản lý quỹ.

  **Response key:** `data`
