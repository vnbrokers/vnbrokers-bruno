info:
  name: Webhook nhận trạng thái từ CTCK.
  type: http
  seq: 13
  tags:
    - Brokers

http:
  method: POST
  url: "{{baseUrl}}/brokers/:brokerId/webhook"
  params:
    - name: brokerId
      value: ""
      type: path
      description: "Id của công ty chứng khoán (VD: Shinhan, BSC...)"
  body:
    type: json
    data: |-
      {
        "type": "",
        "data": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/brokers/:brokerId/webhook"
      method: POST
      params:
        - name: brokerId
          value: ""
          type: path
          description: "Id của công ty chứng khoán (VD: Shinhan, BSC...)"
      body:
        type: json
        data: |-
          {
            "type": "",
            "data": {}
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
