info:
  name: Trả về danh sách công ty chứng khoán
  type: http
  seq: 1
  tags:
    - Brokers

http:
  method: GET
  url: "{{baseUrl}}/brokers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/brokers"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "brokerId": "",
              "name": "",
              "description": "",
              "logo": ""
            }
          ]
