info:
  name: Đăng nhập bằng mật khấu
  type: http
  seq: 2
  tags:
    - Authentication

http:
  method: POST
  url: "{{baseUrl}}/authentication/login"
  body:
    type: json
    data: |-
      {
        "email": "",
        "password": "",
        "rememberMe": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/authentication/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "",
            "password": "",
            "rememberMe": false
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/authentication/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "",
            "password": "",
            "rememberMe": false
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
