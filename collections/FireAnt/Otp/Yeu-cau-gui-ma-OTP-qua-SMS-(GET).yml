info:
  name: Yêu cầu gửi mã OTP qua SMS (GET)
  type: http
  seq: 2
  tags:
    - Otp

http:
  method: GET
  url: "{{baseUrl}}/otp/sms-otp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/otp/sms-otp"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/otp/sms-otp"
      method: GET
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
