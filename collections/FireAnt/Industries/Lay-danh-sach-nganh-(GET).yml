info:
  name: Lấy danh sách ngành (GET)
  type: http
  seq: 4
  tags:
    - Industries

http:
  method: GET
  url: "{{baseUrl}}/industries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/industries"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "industryCode": "",
              "level": 0,
              "name": "",
              "description": ""
            }
          ]
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/industries"
      method: GET
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
