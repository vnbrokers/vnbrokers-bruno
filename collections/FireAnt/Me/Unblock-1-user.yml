info:
  name: Unblock 1 user
  type: http
  seq: 29
  tags:
    - Me

http:
  method: DELETE
  url: "{{baseUrl}}/me/blocked-users/:blockedUserId"
  params:
    - name: blockedUserId
      value: ""
      type: path
      description: Id của user cần unblock
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/me/blocked-users/:blockedUserId"
      method: DELETE
      params:
        - name: blockedUserId
          value: ""
          type: path
          description: Id của user cần unblock
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/me/blocked-users/:blockedUserId"
      method: DELETE
      params:
        - name: blockedUserId
          value: ""
          type: path
          description: Id của user cần unblock
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
