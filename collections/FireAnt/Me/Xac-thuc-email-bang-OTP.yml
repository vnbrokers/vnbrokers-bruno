info:
  name: Xác thực email bằng OTP.
  type: http
  seq: 92
  tags:
    - Me

http:
  method: POST
  url: "{{baseUrl}}/me/broker-user/:brokerId/verify-otp"
  params:
    - name: brokerId
      value: ""
      type: path
      description: "Id của công ty chứng khoán (VD: BSC, Shinhan...)"
  body:
    type: json
    data: |-
      {
        "otpNumber": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/me/broker-user/:brokerId/verify-otp"
      method: POST
      params:
        - name: brokerId
          value: ""
          type: path
          description: "Id của công ty chứng khoán (VD: BSC, Shinhan...)"
      body:
        type: json
        data: |-
          {
            "otpNumber": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "false"
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/me/broker-user/:brokerId/verify-otp"
      method: POST
      params:
        - name: brokerId
          value: ""
          type: path
          description: "Id của công ty chứng khoán (VD: BSC, Shinhan...)"
      body:
        type: json
        data: |-
          {
            "otpNumber": ""
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
