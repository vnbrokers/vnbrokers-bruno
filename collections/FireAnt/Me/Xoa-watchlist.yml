info:
  name: Xóa watchlist
  type: http
  seq: 48
  tags:
    - Me

http:
  method: DELETE
  url: "{{baseUrl}}/me/watchlists/:watchlistId"
  params:
    - name: watchlistId
      value: ""
      type: path
      description: Id của watchlist
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/me/watchlists/:watchlistId"
      method: DELETE
      params:
        - name: watchlistId
          value: ""
          type: path
          description: Id của watchlist
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "0"
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/me/watchlists/:watchlistId"
      method: DELETE
      params:
        - name: watchlistId
          value: ""
          type: path
          description: Id của watchlist
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
