info:
  name: Bỏ theo dõi 1 user
  type: http
  seq: 33
  tags:
    - Me

http:
  method: DELETE
  url: "{{baseUrl}}/me/following/:followedUserId"
  params:
    - name: followedUserId
      value: ""
      type: path
      description: Id của user cần theo dõi
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/me/following/:followedUserId"
      method: DELETE
      params:
        - name: followedUserId
          value: ""
          type: path
          description: Id của user cần theo dõi
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/me/following/:followedUserId"
      method: DELETE
      params:
        - name: followedUserId
          value: ""
          type: path
          description: Id của user cần theo dõi
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
