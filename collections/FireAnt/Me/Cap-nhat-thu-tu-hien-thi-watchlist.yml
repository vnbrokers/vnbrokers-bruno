info:
  name: Cập nhật thứ tự hiển thị watchlist
  type: http
  seq: 49
  tags:
    - Me

http:
  method: PUT
  url: "{{baseUrl}}/me/watchlists/orders"
  body:
    type: json
    data: |-
      {
        "orders": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/me/watchlists/orders"
      method: PUT
      body:
        type: json
        data: |-
          {
            "orders": {}
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/me/watchlists/orders"
      method: PUT
      body:
        type: json
        data: |-
          {
            "orders": {}
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
