info:
  name: Đánh dấu tất cả thông báo là đã đọc
  type: http
  seq: 42
  tags:
    - Me

http:
  method: DELETE
  url: "{{baseUrl}}/me/notifications/unread"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/me/notifications/unread"
      method: DELETE
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/me/notifications/unread"
      method: DELETE
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
