info:
  name: Gửi lời mời
  type: http
  seq: 9
  tags:
    - Rooms

http:
  method: POST
  url: "{{baseUrl}}/rooms/:roomId/invitations"
  params:
    - name: roomId
      value: ""
      type: path
      description: Id nhóm
  body:
    type: json
    data: |-
      {
        "userIds": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/rooms/:roomId/invitations"
      method: POST
      params:
        - name: roomId
          value: ""
          type: path
          description: Id nhóm
      body:
        type: json
        data: |-
          {
            "userIds": []
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/rooms/:roomId/invitations"
      method: POST
      params:
        - name: roomId
          value: ""
          type: path
          description: Id nhóm
      body:
        type: json
        data: |-
          {
            "userIds": []
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
