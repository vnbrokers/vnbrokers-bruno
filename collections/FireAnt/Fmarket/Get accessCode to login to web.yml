info:
  name: Get accessCode to login to web
  type: http
  seq: 6
  tags:
    - Fmarket

http:
  method: POST
  url: "{{baseUrl}}/fmarket/accounts/get-access-code"
  body:
    type: json
    data: |-
      {
        "fmarket_id": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/fmarket/accounts/get-access-code"
      method: POST
      body:
        type: json
        data: |-
          {
            "fmarket_id": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "access_code": ""
          }
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/fmarket/accounts/get-access-code"
      method: POST
      body:
        type: json
        data: |-
          {
            "fmarket_id": ""
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
