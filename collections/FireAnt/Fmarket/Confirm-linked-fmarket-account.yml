info:
  name: Confirm linked fmarket account
  type: http
  seq: 5
  tags:
    - Fmarket

http:
  method: POST
  url: "{{baseUrl}}/fmarket/accounts/link-account/confirm"
  body:
    type: json
    data: |-
      {
        "verification_id": "",
        "verification_token": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/fmarket/accounts/link-account/confirm"
      method: POST
      body:
        type: json
        data: |-
          {
            "verification_id": "",
            "verification_token": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "fmarket_id": ""
          }
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/fmarket/accounts/link-account/confirm"
      method: POST
      body:
        type: json
        data: |-
          {
            "verification_id": "",
            "verification_token": ""
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
