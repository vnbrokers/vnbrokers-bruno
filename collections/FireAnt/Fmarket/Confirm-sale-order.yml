info:
  name: Confirm sale order
  type: http
  seq: 17
  tags:
    - Fmarket

http:
  method: POST
  url: "{{baseUrl}}/fmarket/orders/create-sale-order/confirm"
  body:
    type: json
    data: |-
      {
        "fmarket_id": "",
        "verification_id": "",
        "verification_token": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Thành công
    request:
      url: "{{baseUrl}}/fmarket/orders/create-sale-order/confirm"
      method: POST
      body:
        type: json
        data: |-
          {
            "fmarket_id": "",
            "verification_id": "",
            "verification_token": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "order_code": ""
          }
  - name: 400 Response
    description: Có lỗi khi thực hiện thao tác
    request:
      url: "{{baseUrl}}/fmarket/orders/create-sale-order/confirm"
      method: POST
      body:
        type: json
        data: |-
          {
            "fmarket_id": "",
            "verification_id": "",
            "verification_token": ""
          }
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
