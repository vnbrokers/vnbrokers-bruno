[tools]
pre-commit = "4.6.0"
gitleaks = "8.30.1"

[tasks.pre-commit]
run = "pre-commit run --all-files"

[tasks."gitleaks:dir"]
run = "gitleaks dir -v"

[tasks."gitleaks:git"]
run = "gitleaks git -v -f SARIF"

[tasks.check-leaks]
depends = ["pre-commit", "gitleaks:dir"]
